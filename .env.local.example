# ===========================================
# C-Space HR Platform - Environment Variables
# ===========================================
# Updated: 2026-02-06 (PR2-026 Security Improvement)

# --- Authentication ---
# JWT Secret for token signing/verification
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# CRITICAL: Must be set in production. App will crash without it.
JWT_SECRET=your_jwt_secret_here_64_hex_chars

# --- Supabase Configuration ---
# Get these from: https://app.supabase.com -> Project Settings -> API

# Public URL and anon key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service role key (server-side only, never expose to client)
# IMPORTANT: Rotate this key after initial setup (SEC-004)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# --- Telegram Integration ---
# Bot token for notifications and attendance check-in
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Webhook secret for verifying incoming Telegram updates (SEC-009)
# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
TELEGRAM_WEBHOOK_SECRET=your_telegram_webhook_secret_32_hex_chars

# --- Admin Operations ---
# Secret for the employee import endpoint (SEC-012)
# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
ADMIN_IMPORT_SECRET=your_admin_import_secret_32_hex_chars

# --- Feature Flags ---
# MUST be 'false' in production (SEC-002)
ENABLE_DEMO_USERS=false

# --- Application ---
# Public-facing URL (used for CORS allowlist - SEC-011)
NEXT_PUBLIC_APP_URL=https://niya.cspace.uz
